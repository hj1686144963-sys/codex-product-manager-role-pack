# Codex 五角色自增长工作台

这是由 `codex-product-manager-role-pack 0.2.0` 初始化的通用 Markdown / Obsidian 工作台，不包含任何人的私人历史。

直接在 Codex 中描述任务即可。Codex 会自动按需读取本库，并在产生长期价值时更新相应文件。

核心闭环：

`正常对话 → 自动路由 → 执行或五角色审议 → 验证 → 候选经验 → 去重与批准 → 后续复用`
