# 模型评测工作法

1. 先定义任务、标准输入、预期结果、评分维度和失败样本。
2. 区分字段设计、提示词、模型能力和处理框架，避免把所有问题归因于模型。
3. 每轮只改变一个变量，保留相同样本和相同评分口径。
4. 同时记录 Goodcase、Badcase、耗时、稳定性、成本和可复现条件。
5. 涉及长文本时，同时比较模型与上下文处理框架，不只做模型横向替换。
6. 结论必须区分已验证事实、推断、假设和未知。
