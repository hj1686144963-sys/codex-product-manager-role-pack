# 产品 UI 设计基线

- 当前项目的组件、变量和 Token 是第一真相源。
- 项目没有既有系统时，使用 Arco Design 作为默认主基线；Ant Design 只补充复杂企业交互模式。
- 单页不混用多套视觉 Token。
- 按 `Foundations → 原子组件 → 组合组件 → 业务组件 → 页面模板 → 页面实例` 逐层构建。
- 每层通过 Variant、Auto Layout、长文本、响应式、空/加载/错误/禁用状态、无障碍和截图 QA 后才能晋级。

可执行版安装为 `product-ui-design-baseline` Skill。
